# demo-django
This a mockup project to experiment with news Django features and get some training

In order to run the project, you need to build django a simple `docker-compose up` should do the trick
